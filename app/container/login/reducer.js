import * as _state from './state';

const loginReducer = function (state = _state.loginState, action) {
    switch (action.type) {
        default:
            return state;
    }
};

export { loginReducer };
